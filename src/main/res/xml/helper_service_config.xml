<?xml version="1.0" encoding="utf-8"?>
<!--
  helper_service_config.xml — AccessibilityService configuration v6

  canRetrieveWindowContent="true"  → getRootInActiveWindow() enable
  canPerformGestures="true"        → dispatchGesture() enable (tap/swipe/pinch)
  canRequestFilterKeyEvents="true" → key event capture (optional)

  packageNames restricted to CapCut package variants only —
  reduces unnecessary a11y callbacks from other apps.
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"

    android:description="@string/helper_description"
    android:settingsActivity="com.r4x.capcut_helper.SettingsActivity"

    android:accessibilityEventTypes="
        typeWindowStateChanged|
        typeWindowContentChanged|
        typeViewScrolled|
        typeViewClicked|
        typeViewFocused"

    android:accessibilityFeedbackType="feedbackGeneric"

    android:accessibilityFlags="
        flagReportViewIds|
        flagRetrieveInteractiveWindows|
        flagRequestTouchExplorationMode|
        flagRequestFilterKeyEvents|
        flagRequestEnhancedWebAccessibility"

    android:canRetrieveWindowContent="true"
    android:canPerformGestures="true"
    android:canRequestFilterKeyEvents="true"
    android:canRequestTouchExplorationMode="true"

    android:notificationTimeout="50"

    android:packageNames="com.lemon.lvoverseas,com.lemon.lv,com.ss.android.ugc.aweme"
    />
